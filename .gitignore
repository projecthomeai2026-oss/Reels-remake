node_modules
dist
.DS_Store
.env

# Ignore the output video from Git but not videos you import into src/.
out

# Тяжёлые медиа — не помещаются в GitHub (лимит 100MB/файл), храним локально
public/clips
audio
tmp.json
ffmpeg2pass-*
whisper.cpp
